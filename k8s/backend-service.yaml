apiVersion: v1
kind: Service
metadata:
  name: waw-backend
  namespace: default
spec:
  selector:
    app: waw-backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30081
  type: NodePort